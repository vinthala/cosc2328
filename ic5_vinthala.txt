1. An external stylesheet outside of the html itself, but is in a file linked to the html file that codes the style of the file; it is better to keep css in its own file than putting it inside of the HTML because it more readable, decreases bandwidth, and because we can change what is in the css folder seperately which will make it easier to keep track of.
2. A selector is something that selects the element while the declaration is the code inside of it that tells the property of the selected item what value it should have. One example from the file is the color and the color code.
3. One difference between a class selector and an id selector is that an id selector is unique while a class selector is not. I would use a class selector card when dealing with something less specific and use the id selector when dealing with an exact file/image/object.
4. The <link> tag connects the page (ic5.html) to the stylesheet, the css/ic5.css connects it to where ic5.css is at the same level as ic5.html, but css is in the a subfolder next to the subfolder that holds ic5.html, <img> links ic5.html to the subfolder images, images/photo directs ic5.html into a image inside of the subfolder images.
